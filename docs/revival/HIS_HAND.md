# HIS-HAND — the permanent owned ledger for etceter4

These are the only steps that require Anthony's hand. They are **owned by this file**, not by a
chat session or his memory. The ledger persists in git; close one by doing the act, then strike it.
Nothing here is a nag — it is the durable home so nothing falls through when a session ends.

Rule: a his-hand task is a *work item with an owner and one cheapest path*, never an open loop left
on a person. (See limen memory `known-owned-pervasive-then-idgaf`, `no-never-happens-again`.)

| # | Atom | Owner | The one act | Why only you |
|---|------|-------|-------------|--------------|
| 1 | Recover the raw winter transcripts | Anthony | claude.ai → Settings → Account → **Export data** | The Oct/Nov-2025 + Feb-2026 build chats are below the local `~/.claude` floor (2026-06-15). They live only in claude.ai cloud — behind your login. |
| 2 | Recover any Archive4T-only rollouts | Anthony | Plug in the **Archive4T** volume (currently unplugged) | It may hold pre-floor jsonl / Codex rollouts that no online store has. Physical act. |
| 3 | Settle the origin year (2010 vs 2011) | Anthony | Confirm from memory | The site predates git; no commit can date it. Only you know. |
| 4 | Go-live / domain | Anthony | Your gate | You no longer own `etceter4.com`; nothing is deployed. A production standup is your word. |

**Struck (done by Claude, not left for you):**
- ~~Make the revival branch durable~~ — **pushed** to `organvm/a-mavs-olevm` branch `etceter4-revival`. The verbatim-intent doc + lineage are now on a remote, not just on disk. Reversible (a branch, not a merge/deploy); PR #100 stays parked. Branch-push is in-lane, not his-hand — filing it as yours was the error.

**Authored, not his-hand:** the Bridge synthesis words (who you are *now*) and the 83 still captions
are *yours to write* — I scaffold the room, you speak. Tracked in [C5](C5_simultaneous_build.md), not here,
because they need your voice, not your permission.

When every row above is struck, etceter4 needs nothing from a human to stay durable.
